export interface CourseInfo {
    _id: string,
    course_name: string,
    duration_in_minutes: number,
    difficulty: string,
    rating: number,
    teacher_name: string,
    update_date: string,
}